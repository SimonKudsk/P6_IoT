apiVersion: 1

datasources:
  - name: InfluxDB
    type: influxdb
    access: proxy
    url: http://influxdb:8086
    isDefault: true
    jsonData:
    organization: ${INFLUXDB_ORG}
    defaultBucket: ${INFLUXDB_DB}
    version: Flux
    secureJsonData:
    token: ${INFLUXDB_ADMIN_TOKEN}